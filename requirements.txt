# Core dependencies (all platforms)
sounddevice>=0.4.6
numpy>=1.24.0
websockets>=12.0
openai>=1.30.0
certifi>=2024.0.0

# System tray + icon (cross-platform)
pystray>=0.19.0
pillow>=9.0.0

# macOS tray backend (pystray uses AppKit on macOS)
pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"
